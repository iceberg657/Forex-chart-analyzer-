
import React from 'react';

const Charting: React.FC = () => {
  // The TradingViewWidget is now rendered persistently in App.tsx
  // This component acts as a route placeholder to ensure the correct layout (Header/Nav) is shown.
  return null;
};

export default Charting;
